# Goal: Define your product offer + SKUs that map cleanly to Stripe

- [ ] Write the 1-sentence product promise (what buyer gets).
- [ ] Name the first paid product (internal key + marketing name).
- [ ] Define exactly what’s included (bullet list).
- [ ] Decide pricing model for MVP (one-time only).
- [ ] Create Stripe Product + Price in test mode.
- [ ] Local test: store `STRIPE_PRICE_ID` in `.env.local` and verify checkout session creation uses it.