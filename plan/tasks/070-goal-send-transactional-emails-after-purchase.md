# Goal: Send transactional emails after purchase (optional)

- [ ] Add Resend SDK and env vars.
- [ ] Send a purchase confirmation email after entitlement is granted (in webhook handler or async job).
- [ ] Add a simple email template with links back to `/library`.
- [ ] Local test: use Resend dev/testing mode (or log output) to verify send is called once.