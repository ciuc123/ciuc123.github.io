# Goal: Launch and validate first sale end-to-end

- [ ] Create a “production checklist” doc (env vars, webhook secret, domain).
- [ ] Deploy to production (Vercel) and verify routing.
- [ ] Switch Stripe to live mode only when ready (keep test mode working in preview).
- [ ] Do a $1 test purchase (if you choose) or a real purchase yourself.
- [ ] Verify: purchase → entitlement → library access → email (if enabled).
- [ ] Local test: none (this is production validation), but record each step as notes.