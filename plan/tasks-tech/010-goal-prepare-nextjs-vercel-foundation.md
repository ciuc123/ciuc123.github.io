# Goal: Prepare a Next.js + Vercel-ready foundation

- [ ] Inspect current repo structure and note what is static site vs app code.
- [ ] Add/confirm Node version + package manager choice (document in README or .nvmrc).
- [ ] Add Next.js (App Router) skeleton that runs with `npm run dev`.
- [ ] Add `npm run build` and ensure it succeeds locally.
- [ ] Add a placeholder route `/library` that renders a simple page.
- [ ] Local test: run `npm install`, `npm run dev`, open `/` and `/library`.